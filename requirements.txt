# Runtime dependencies, pinned for reproducible installs.
# Refresh with: pip install -U <pkg> && pip freeze | grep -i <pkg>

flask>=3.1.3
requests>=2.32.3
waitress>=3.0.2
cryptography>=50.0.1
keyring>=25.7.0

# Development only:
pytest>=9.1.1
