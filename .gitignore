# Data and credentials live in ~/.plutus, never here.
*.db
*.db-*
.env
__pycache__/
*.pyc
venv/
.venv/
.pytest_cache/
.fuse_hidden*

# Claude Code local state - sessions, worktrees - minus the launch profile,
# which is checked in.
.claude/*
!.claude/launch.json
